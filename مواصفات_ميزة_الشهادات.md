# مواصفات تقنية: إكمال الدروس + إصدار الشهادات تلقائيًا
### منصة إيليت (ELITE) — eliteco.com.kw

---

## 1. نظرة عامة على الميزة

- كل درس فيديو بالدورة يظهر تحته checkbox "أنهيت مشاهدة هذا الدرس"، **معطّل** حتى يوصل الفيديو لنهايته (بغض النظر إن كان تشغيل طبيعي أو Seek/Skip لآخر الفيديو).
- بعد ما الطالب يضغط checkbox كل دروس الدورة، يصدر النظام تلقائيًا **شهادة إتمام** بصيغة PDF، مبنية على قالب تصميم جاهز (نسخة للذكور ونسخة للإناث)، معبّاة تلقائيًا ببيانات الطالب والدورة والتاريخ.
- الاسم يُسأل من الطالب بكل عملية شراء دورة (يمكن يختلف باسم مختلف بكل دورة). الجنس يُحدَّد مرة وحدة وقت إعداد الحساب.

---

## 2. تعديلات قاعدة البيانات (Supabase / PostgreSQL)

### 2.1 جدول جديد: `lesson_progress`
يسجّل إكمال كل درس لكل طالب.

```sql
create table lesson_progress (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  lesson_id uuid not null references lessons(id) on delete cascade,
  completed_at timestamptz not null default now(),
  unique (user_id, lesson_id)
);

-- RLS: كل مستخدم يشوف/يضيف سجلاته هو بس
alter table lesson_progress enable row level security;

create policy "user can view own progress"
  on lesson_progress for select
  using (auth.uid() = user_id);

create policy "user can insert own progress"
  on lesson_progress for insert
  with check (auth.uid() = user_id);
```

> ملاحظة: ما نحتاج UPDATE أو DELETE policy — الإكمال إجراء نهائي (upsert بس، عبر `on conflict do nothing`).

### 2.2 إضافة عمود الجنس لجدول `users`

```sql
alter table users
  add column gender text check (gender in ('male', 'female'));
```

- يُسأل بنموذج إعداد الحساب (Signup أو صفحة إكمال البروفايل بعد أول تسجيل دخول).
- إلزامي قبل ما يقدر يشتري أي دورة (عشان نعرف أي قالب شهادة نستخدم لاحقًا).

### 2.3 إضافة عمود اسم الشهادة لجدول `enrollments`

```sql
alter table enrollments
  add column certificate_name text;
```

- يُعبّى بنموذج الشراء/التسجيل بالدورة (سؤال منفصل: "الاسم كما تريده بالشهادة").

### 2.4 إضافة توقيع المدرب لجدول `courses`

```sql
alter table courses
  add column trainer_signature_url text;
```

- صورة يرفعها الأدمن (أو المدرب نفسه إذا سمحتوا) وقت إنشاء/تعديل الدورة، تُخزَّن بـ Supabase Storage ويُحفظ رابطها هنا.

### 2.5 تعديل جدول `certificates` الموجود

تأكد إنه فيه الأعمدة التالية (إضافة الناقص فقط):

```sql
alter table certificates
  add column if not exists pdf_url text,
  add column if not exists issued_at timestamptz default now();
```

---

## 3. منطق إكمال الفيديو (Frontend)

بصفحة مشغّل الفيديو (React component):

```javascript
const [videoEnded, setVideoEnded] = useState(false);

<video
  onEnded={() => setVideoEnded(true)}
  onSeeked={(e) => {
    // لو المستخدم عمل seek لآخر الفيديو مباشرة (خلاص مو محتاج يشوفه)
    if (e.target.currentTime >= e.target.duration - 0.5) {
      setVideoEnded(true);
    }
  }}
  ...
/>

<input
  type="checkbox"
  disabled={!videoEnded}
  onChange={handleMarkComplete}
/>
```

- `handleMarkComplete` يستدعي API route `/api/lessons/[lessonId]/complete` (POST) يسجّل بجدول `lesson_progress`.
- بعد كل استدعاء ناجح، الـ API يتحقق: هل عدد صفوف `lesson_progress` لهذا الطالب بنفس الدورة = عدد كل دروس الدورة؟ لو نعم → يستدعي دالة إصدار الشهادة (قسم 4).

---

## 4. منطق إصدار الشهادة (Backend, server-only)

### 4.1 المكتبة المقترحة
`pdf-lib` (تدعم تحميل صورة PNG كخلفية + كتابة نص عربي RTL فوقها بإحداثيات دقيقة). بديل: توليد الصورة عبر `@napi-rs/canvas` بالسيرفر ثم تحويلها لـ PDF.

> ⚠️ النص العربي: تأكد المكتبة تدعم شكل الحروف المتصلة (RTL shaping) — `pdf-lib` وحدها ما تكفي لعرض عربي صحيح، يفضّل استخدام مكتبة مساعدة مثل `arabic-reshaper` + خط عربي مضمّن (TTF) بالسيرفر قبل الكتابة على القالب.

### 4.2 خطوات التوليد

```
1. اجلب بيانات: اسم الطالب (certificate_name)، اسم الدورة، اسم المدرب،
   جنس الطالب (لاختيار القالب)، تاريخ اليوم (اسم اليوم بالعربي + التاريخ)،
   رابط توقيع المدرب من courses.trainer_signature_url.
2. اختر القالب: male_template.png أو female_template.png حسب users.gender
3. حمّل القالب كخلفية PDF (صفحة واحدة، بنفس أبعاد الصورة: 2000×1414)
4. اكتب النصوص بالإحداثيات التالية (تقريبية — تحتاج ضبط بصري نهائي):

   | الحقل                          | x         | y    |
   |--------------------------------|-----------|------|
   | اسم الطالب/ة                    | 1000-1750 | 690  |
   | اسم الدورة                      | 950-1500  | 790  |
   | اسم المدرب (بعد "تقديم:")        | 700-950   | 850  |
   | اسم اليوم (بعد "يوم")            | 1400-1550 | 850  |
   | التاريخ (بعد "الموافق:")         | 950-1300  | 910  |
   | صورة توقيع المدرب                | 350-650   | 1180-1250 |

5. ألصق صورة توقيع المدرب (trainer_signature_url) بمكانها
6. صدّر PDF، ارفعه لـ Supabase Storage (bucket خاص: certificates/)
7. سجّل صف بجدول certificates: user_id, course_id, pdf_url, issued_at
```

### 4.3 اسم اليوم بالعربي تلقائيًا

```javascript
const days = ['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت'];
const dayName = days[new Date().getDay()];
// التاريخ بصيغة عربية: مثلاً 26/7/2026
```

---

## 5. نقاط تحتاج ضبط بصري نهائي (لا يمكن تحديدها بدون تجربة فعلية)

- الإحداثيات المذكورة بقسم 4.2 **تقريبية** (مأخوذة من فحص بصري للصورة، ±20-30px). لازم تجربة فعلية: كتابة نص تجريبي بهالمواقع وتصحيح العين، خصوصًا لأسماء طويلة قد تخرج عن المساحة المخصصة.
- حجم/نوع الخط العربي المستخدم بالتصميم الأصلي (لمطابقته بالنص المُولَّد).
- سلوك الاسم الطويل: تصغير حجم الخط تلقائيًا (auto-shrink) إذا الاسم أطول من عرض معيّن، أو قص/التفاف لسطرين.

---

## 6. ملخص الملفات/المسارات الجديدة المطلوبة

- `supabase/lesson_progress.sql` — الجدول + RLS
- `supabase/certificates_and_courses_updates.sql` — أعمدة gender, certificate_name, trainer_signature_url
- `/api/lessons/[lessonId]/complete` — POST، يسجل الإكمال ويتحقق من اكتمال الدورة
- `/api/certificates/generate` — دالة مساعدة (تُستدعى داخليًا من نقطة أعلاه) لتوليد PDF
- `/public/certificate-templates/male_template.png` و `female_template.png` — القوالب اللي رفعتها
- تعديل نموذج Signup/إكمال البروفايل لإضافة حقل الجنس
- تعديل نموذج شراء الدورة لإضافة حقل "الاسم كما تريده بالشهادة"
- تعديل نموذج إضافة/تعديل الدورة (لوحة الأدمن) لإضافة رفع توقيع المدرب

---

## 7. ملاحظة أمنية

كل منطق التحقق من "هل الطالب أكمل كل الدروس فعلاً" يجب يصير **بالسيرفر فقط** (API route)، وليس بالواجهة — تمامًا بنفس المبدأ المطبّق على مسار الأدمن ومنطق UPayments بالمشروع.
