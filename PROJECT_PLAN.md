# خطة مشروع منصة إيليت التدريبية

## نظرة عامة
تحويل موقع إيليت الحالي (الثابت على GitHub Pages) إلى منصة تعليمية متكاملة تدعم:
- تسجيل دخول بدورين (مستخدم / مدرب)
- داشبورد خاص للمدربين
- لوحة تحكم أدمن مخفية
- نظام دفع عبر UPayments

## البنية التقنية المختارة (مجانية بالبداية)

| المكوّن | التقنية | السبب |
|---|---|---|
| الواجهة الأمامية | Next.js | يدعم صفحات متعددة الأدوار + API Routes مدمجة |
| قاعدة البيانات + المصادقة | Supabase (الخطة المجانية) | Auth جاهز (إيميل/Google/Apple) + قاعدة بيانات PostgreSQL + تخزين ملفات |
| الاستضافة | Vercel (الخطة المجانية) | تكامل مباشر مع Next.js + ربط دومين مجاني |
| الدفع | UPayments API | يُستدعى فقط من الخادم (API Routes) — المفتاح لا يظهر أبدًا للمتصفح |

✅ **جميع المكوّنات مجانية بالبداية** ضمن الحدود المعقولة لمنصة ناشئة.

---

## المراحل (بالترتيب المتفق عليه)

### 🔹 المرحلة 1: نظام تسجيل الدخول (Auth) ✅ منجزة
- إعداد مشروع Supabase جديد
- تفعيل تسجيل دخول: إيميل/باسورد، Google
- إنشاء جدول `users` بحقل `role` (student / trainer / admin / staff)
- صفحة تسجيل دخول وتسجيل حساب موحّدة، مع اختيار الدور عند التسجيل
- حماية المسارات (Route Protection) حسب الدور
- حقل `gender` (male/female) بجدول `users` — يُسأل مرة وحدة عند إعداد الحساب (مطلوب للشهادة)
- Google OAuth مع `prompt: 'select_account'` عشان يسأل عن الحساب كل مرة بدون تسجيل تلقائي

---

### 🔹 المرحلة 2: الداشبوردات ✅ منجزة

**داشبورد المستخدم (Student):**
- قائمة دوراته المسجل فيها
- شهاداته (عرض/تحميل)
- إمكانية كتابة تعليقات على الدورات

**داشبورد المدرب (Trainer):**
- عدد المشتركين لكل دورة
- إجمالي الدخل لكل دورة (وإجمالي عام)
- صندوق رسائل: استقبال DM من المشتركين + الرد عليهم

**جداول قاعدة بيانات مطلوبة:**
`courses`, `enrollments`, `certificates`, `comments`, `messages`

---

### 🔹 المرحلة 3: لوحة تحكم الأدمن ✅ منجزة
- مسار غير معلن: `/control-panel-2026`
- حماية: يتحقق من `role = admin` بكل طلب من السيرفر
- إدارة الدورات: إضافة/تعديل/حذف
- تحديد السعر والخصومات لكل دورة
- عرض إحصائيات عامة (عدد المستخدمين، الدخل الكلي، أكثر الدورات مبيعًا)
- إدارة دروس كل دورة من `/control-panel-2026/courses/[courseId]/lessons`
- إدارة مواضيع الأعضاء (رفع PDF + عنوان + اسم صاحب الموضوع + تاريخ)
- تقييمات staff الشهرية من `/control-panel-2026/evaluations`
- robots.txt يمنع Google من اكتشاف `/control-panel-2026`

---

### 🔹 المرحلة 4: نظام الدفع (UPayments) ✅ منجزة (Mock Mode)
- API Route خاص بالخادم يستقبل طلب الدفع
- استدعاء UPayments API من الخادم فقط
- عند نجاح الدفع: تسجيل الاشتراك في `enrollments` + تحديث دخل المدرب
- تطبيق الخصومات إذا كانت مفعّلة
- حقل `certificate_name` بجدول `enrollments` — يُسأل عند كل شراء دورة
- متغيرات البيئة المطلوبة: `UPAYMENTS_API_KEY`, `UPAYMENTS_BASE_URL`, `UPAYMENTS_MOCK_MODE`
- Sandbox Base URL: `https://sandboxapi.upayments.com/api/v1`

⚠️ **قاعدة أمان حرجة:** مفتاح UPayments لا يوضع في كود الواجهة أو يُرفع على GitHub.

**المتبقي:** الحصول على API Key حقيقي من UPayments (merchant.upayments.com) للانتقال من Mock لـ Production.

---

### 🔹 المرحلة 5: الدومين والإطلاق ✅ منجزة
- الدومين `eliteco.com.kw` مربوط عبر Cloudflare + Vercel
- Google OAuth Redirect URI: `https://eliteco.com.kw/auth/callback`
- Supabase Site URL: `https://eliteco.com.kw`
- `NEXT_PUBLIC_SITE_URL=https://eliteco.com.kw` على Vercel

---

### 🔹 المرحلة 6: نظام staff (موظفين/إداريين) ✅ منجزة
- دور جديد "staff" منفصل عن trainer
- أقسام/إدارات (departments) — الأقسام الستة المضافة بقاعدة البيانات:
  1. المدير/الرئيس (صلاحية إعطاء مهام لكل الأقسام)
  2. الموارد البشرية
  3. الأرشفة
  4. العلاقات العامة
  5. الإعلامية
  6. التدريب والتطوير
- صلاحيات: عضو "المدير/الرئيس" يعطي مهام لكل الأقسام، رئيس أي قسم آخر يعطي مهام لقسمه فقط (`is_director_dept = true` بجدول `departments`)
- ملفات شخصية لكل عضو + ملفات مشتركة لكل قسم
- نوت شخصي بمحرر نصوص غني (Bold، حجم/نوع الخط) قابل للتحميل (txt/PDF)
- مهام القسم: عنوان/وصف/تاريخ استحقاق/checkbox، تُعيّن لعضو محدد وتظهر له فقط
- تقييم شهري من الأدمن (رقم 1-12 + بونس 0-2 + ملاحظة)، العضو يشوفه بس ما يعدّله

---

### 🔹 المرحلة 7: قسم "مواضيع الأعضاء" ✅ منجزة
- يظهر تحت قسم "مقالات ومعرفة" بالموقع
- كل موضوع: عنوان + اسم صاحب الموضوع + تاريخ النشر + PDF قابل للعرض داخل الموقع
- الأدمن يضيف المواضيع من `/control-panel-2026`
- جدول `member_topics`: id, title, author_name, published_at, pdf_url, created_at
- مخفي من الصفحة الرئيسية، يظهر فقط عبر قائمة الـ 3 شخطات

---

### 🔹 المرحلة 8: دروس الفيديو عبر Bunny Stream ✅ منجزة
- كل دورة تحتوي دروس فيديو، الأدمن فقط يديرها
- حماية عبر روابط تشغيل موقّعة (SHA256 token، صالحة لساعتين) + RLS على جدول `lessons`
- Token Authentication مفعّل بـ Bunny Stream (اختُبر فعلياً — 403 بدون token صحيح)
- شراء الدورات مفتوح لكل الأدوار (student/trainer/staff/admin)
- متغيرات البيئة: `BUNNY_STREAM_LIBRARY_ID`, `BUNNY_STREAM_TOKEN_AUTH_KEY`

---

### 🔹 المرحلة 9: شهادة الإتمام التلقائية ✅ منجزة
- checkbox "أنهيت مشاهدة هذا الدرس" تحت كل فيديو — معطّل حتى يصل الفيديو لنهايته (onEnded أو Seek لآخره عبر Bunny Player.js)
- بعد إكمال كل دروس الدورة تصدر شهادة PDF تلقائياً
- جدول `lesson_progress`: user_id, lesson_id, completed_at
- التحقق من اكتمال الدروس يصير بالسيرفر فقط عبر `/api/lessons/[lessonId]/complete`
- **تصميم الشهادة**: HTML → PDF عبر Puppeteer (Cairo)، تصميم واحد للجميع، الفرق بالنص فقط:
  - ذكر: "الأستاذ:" + "أتم دورة"
  - أنثى: "الأستاذة:" + "أتمت دورة"
- **الألوان الرسمية**: أزرق `#4B96CE`، أخضر `#87C781`، ذهبي `#C9A84C`
- **عناصر الشهادة**: شعار إيليت واترمارك، ختم الإدارة فوق توقيع الإدارة (يتداخلان)، ميدالية، بدون توقيع مدرب
- **الحقول الديناميكية**: اسم الطالب (من `certificate_name` بـ enrollments)، اسم الدورة، اسم المدرب، اليوم بالعربي، التاريخ بصيغة DD/M/YYYY
- الشهادة تُرفع لـ Supabase Storage وتُسجّل بجدول `certificates`
- ملفات القوالب: `/public/certificate-assets/` (stamp.png, admin-signature.png, medal.png, logo-watermark.png)

---

### 🔹 المرحلة 10: تحسينات الواجهة ✅ منجزة
- إخفاء هذه العناصر من شريط التنقل والصفحة الرئيسية، تظهر فقط عبر قائمة الـ 3 شخطات:
  - الأعضاء
  - الاعتماد المهني المستمر
  - مقالات ومعرفة
  - مواضيع الأعضاء
  - قسم فريق إيليت الإداري من الصفحة الرئيسية
- أيقونة الـ 3 شخطات (hamburger) موجودة دائماً بالشريط العلوي (desktop + mobile)
- الهيكل التنظيمي لفريق إيليت:
  - محمد الهلال (الرئيس)
    - لولوه البندر → علي الساعدي → عمر + ريم
    - محمد حسين → إيمان الظفيري → نورة السبيعي
    - محمد حسين → أحمد البناي

---

## المتبقي قبل الإطلاق الكامل
- الحصول على API Key Production من UPayments وتغيير `UPAYMENTS_MOCK_MODE` لـ `false`
- تجربة دورة كاملة تجريبية (3 فيديوهات) للتأكد من كل الرحلة
- تعديل النصوص والمحتوى بالموقع
- التأكد إن مسار تغيير الأدوار المؤقت مقفول قبل الإطلاق للجمهور

---

## ملاحظات عامة
- كل مرحلة تُبنى وتُختبر لحالها قبل الانتقال للي بعدها
- التطوير الفعلي يصير عبر **Claude Code** على الجهاز (هذا الملف + CLAUDE.md يعطونه السياق الكامل)
- بعد كل مرحلة، ينفع نرفع التحديثات على GitHub (commit) قبل الانتقال للمرحلة التالية
- صاحب المشروع مبتدئ بالبرمجة — اشرح كل خطوة بشكل مبسّط واسأل قبل أي تغيير كبير
