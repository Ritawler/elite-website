# تعليمات مشروع منصة إيليت (ELITE)

## 🎯 المرحلة الحالية
كل المراحل والميزات الأساسية مكتملة ومشغّلة (تسجيل الدخول، الداشبوردات، لوحة الأدمن، الدفع UPayments بوضع Mock، الدومين eliteco.com.kw شغّال، نظام staff الكامل، مواضيع الأعضاء بـ PDF viewer، دروس الفيديو عبر Bunny Stream، إكمال الدروس + شهادات PDF تلقائية، شراء الدورات لكل الأدوار، قائمة الأقسام الستة، تحسينات الواجهة).

**المتبقي الوحيد:** الحصول على API Key Production من UPayments للانتقال من Mock Mode للدفع الفعلي.

صاحب المشروع مبتدئ بالبرمجة — اشرح كل خطوة بشكل مبسط قبل تنفيذها، واسأله قبل أي تغيير كبير أو حذف ملفات.

## عن المشروع
منصة تدريبية إلكترونية لمنظمة "إيليت" الكويتية (تدريب في علم النفس والتربية والتطوير الذاتي). الموقع الحالي ثابت (HTML/CSS/JS) ومستضاف على GitHub Pages، والهدف تحويله لمنصة كاملة فيها تسجيل دخول، دورات، دفع إلكتروني.

## اللغة والاتجاه
- المحتوى بالكامل عربي، اتجاه الصفحة RTL
- حافظ على نفس أقسام الموقع الأصلي: الرئيسية، عن إيليت، الأعضاء، الدورات، الاعتماد المهني، الفعاليات، بودكاست (استبصار)، المدونة، تواصل معنا
- لا تغيّر معلومات التواصل الموجودة (واتساب، إيميل، حسابات التواصل) إلا بطلب صريح

## البنية التقنية
- Next.js للواجهة + API Routes
- Supabase لقاعدة البيانات والمصادقة (Auth)
- Vercel للاستضافة
- UPayments API للدفع — **يُستدعى من الخادم فقط، أبدًا من كود المتصفح**
- Bunny Stream لاستضافة وتشغيل فيديوهات دروس الدورات

## الأدوار (Roles)
1. **student**: يشوف دوراته، شهاداته، يكتب تعليقات
2. **trainer**: داشبورد خاص (عدد المشتركين، الدخل لكل دورة، رسائل DM من الطلاب)
3. **admin**: مسار مخفي غير معلن، يدير الدورات/الأسعار/الخصومات، يشوف كل الإحصائيات
4. **staff**: أعضاء إدارة/موظفين — دور منفصل تمامًا عن trainer، له قسم خاص فيه:
   - مساحة ملفات خاصة بكل عضو
   - نوت/محرر نصوص شخصي غني (Bold، حجم الخط، نوع الخط) قابل للتحميل (txt/PDF)
   - **أقسام/إدارات (departments):** الأقسام الستة المضافة بقاعدة البيانات:
     1. المدير/الرئيس — `is_director_dept = true` (صلاحية إعطاء مهام لكل الأقسام)
     2. الموارد البشرية
     3. الأرشفة
     4. العلاقات العامة
     5. الإعلامية
     6. التدريب والتطوير
   - ملفات القسم: خاصة بأعضاء نفس القسم + الأدمن
   - **مهام القسم:** عنوان/وصف/تاريخ استحقاق/checkbox، تُعيّن لعضو محدد وتظهر له فقط
   - **تقييم شهري:** الأدمن يضيف (1-12 + بونس 0-2 + ملاحظة)، العضو يشوفه فقط من `/dashboard/staff`

## شراء الدورات مفتوح لكل الأدوار
أي حساب (student, trainer, staff, admin) يقدر يشتري أي دورة. الدورات تظهر بداشبورد كل دور بقسم "دوراتي" الخاص به.

## دروس الدورات — فيديو عبر Bunny Stream ✅ مكتمل
- الأدمن فقط يدير الدروس من `/control-panel-2026/courses/[courseId]/lessons`
- الحماية: روابط تشغيل موقّعة (SHA256 token، صالحة لساعتين) + RLS على جدول `lessons`
- Token Authentication مفعّل بـ Bunny Stream ومختبر (403 بدون token صحيح)
- كشف انتهاء الفيديو عبر Bunny **Player.js** (مو `onEnded` — لأن المشغّل iframe خارجي)
- متغيرات البيئة: `BUNNY_STREAM_LIBRARY_ID`, `BUNNY_STREAM_TOKEN_AUTH_KEY`

## إكمال الدروس + شهادات إتمام تلقائية ✅ مكتمل
- checkbox "أنهيت مشاهدة هذا الدرس" تحت كل فيديو — معطّل حتى ينتهي الفيديو عبر Player.js
- بعد إكمال كل دروس الدورة → شهادة PDF تلقائية
- جدول `lesson_progress`: user_id, lesson_id, completed_at
- التحقق بالسيرفر فقط: `/api/lessons/[lessonId]/complete`
- **تصميم الشهادة**: HTML → PDF عبر Puppeteer (Cairo)، تصميم واحد للجميع:
  - ذكر: "الأستاذ:" + "أتم دورة" | أنثى: "الأستاذة:" + "أتمت دورة"
  - الألوان: أزرق `#4B96CE`، أخضر `#87C781`، ذهبي `#C9A84C`
  - عناصر: واترمارك شعار إيليت، ختم الإدارة فوق توقيع الإدارة (يتداخلان)، ميدالية
  - **بدون** توقيع مدرب
- **الجنس** (`users.gender`): يُسأل ببوابة قبل الشراء مباشرة (يغطي الحسابات القديمة)
- **اسم الشهادة** (`certificate_name`): يُسأل بكل عملية شراء دورة
- ملفات الشهادة: `/public/certificate-assets/` (stamp.png, admin-signature.png, medal.png, logo-watermark.png)
- الشهادة تُرفع لـ Supabase Storage وتُسجّل بجدول `certificates`

## قسم "مواضيع الأعضاء" ✅ مكتمل
- يظهر تحت قسم "مقالات ومعرفة" بالموقع
- كل موضوع: عنوان + اسم صاحب الموضوع + تاريخ النشر + PDF viewer داخل الموقع
- الأدمن يضيف المواضيع من `/control-panel-2026`
- جدول `member_topics`: id, title, author_name, published_at, pdf_url, created_at
- مخفي من الصفحة الرئيسية، يظهر فقط عبر قائمة الـ 3 شخطات

## تحسينات الواجهة ✅ مكتمل
- هذه العناصر مخفية من الصفحة الرئيسية وشريط التنقل، تظهر فقط عبر قائمة الـ 3 شخطات:
  - الأعضاء، الاعتماد المهني المستمر، مقالات ومعرفة، مواضيع الأعضاء
  - قسم فريق إيليت الإداري كاملاً
- أيقونة الـ 3 شخطات موجودة دائماً (desktop + mobile)
- Google OAuth مع `prompt: 'select_account'` — يسأل عن الحساب كل مرة
- الهيكل التنظيمي: محمد الهلال (رئيس) ← لولوه البندر / محمد حسين ← باقي الفريق

## ترقية الحساب من student إلى trainer
- يحتاج موافقة جهة مخوّلة (أدمن أو حساب يمنحه الأدمن الصلاحية)
- جدول طلبات الترقية بحالة pending/approved/rejected
- **وضع مؤقت:** تغيير الدور يدوياً من SQL Editor لأغراض الاختبار فقط — **لازم يُقفل قبل الإطلاق للجمهور**

## قواعد أمان صارمة
- مفتاح UPayments API في متغيرات البيئة فقط — **ممنوع بأي ملف كود أو GitHub**
- `.env.local` مذكور في `.gitignore`
- مسار الأدمن يتحقق من `role === 'admin'` بكل طلب من السيرفر
- بيانات الدفع ومفاتيح API لا تظهر بـ DevTools
- `robots.txt` يمنع Google من اكتشاف `/control-panel-2026`

## حالة المشروع الحالية
- [x] المرحلة 1: نظام تسجيل الدخول (إيميل/Google + الأدوار + gender field)
- [x] المرحلة 2: داشبورد المستخدم والمدرب
- [x] المرحلة 3: لوحة تحكم الأدمن
- [x] المرحلة 4: تكامل الدفع UPayments (Mock Mode — بانتظار API Key Production)
- [x] المرحلة 5: ربط الدومين eliteco.com.kw
- [x] نظام staff بالكامل (الأقسام الستة، الملفات، النوت الغني، PDF، مهام القسم، التقييم الشهري)
- [x] مواضيع الأعضاء (PDF viewer تحت مقالات ومعرفة)
- [x] دروس الدورات عبر Bunny Stream (Token Authentication مفعّل ومختبر)
- [x] فتح شراء الدورات لكل الأدوار
- [x] إكمال الدروس + شهادات إتمام تلقائية (SQL مشغّل، مكتمل)
- [x] تحسينات الواجهة (hamburger menu، إخفاء عناصر، هيكل تنظيمي، Google OAuth)

راجع ملف `PROJECT_PLAN.md` للتفاصيل الكاملة لكل مرحلة قبل البدء بأي جزء.

## ملاحظة للمطوّر (مبتدئ)
صاحب المشروع مبتدئ بالبرمجة — اشرح كل خطوة تسويها بشكل مبسّط، واسأل قبل تنفيذ أي تغيير كبير أو حذف بيانات.
