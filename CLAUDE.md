# تعليمات مشروع منصة إيليت (ELITE)

## 🎯 المرحلة الحالية
كل المراحل والميزات الأساسية مكتملة ومشغّلة على قاعدة البيانات الحيّة (تسجيل الدخول، الداشبوردات، لوحة الأدمن، الدفع UPayments بوضع Mock، الدومين eliteco.com.kw شغّال، نظام staff الكامل، مواضيع الأعضاء، دروس الفيديو عبر Bunny Stream، إكمال الدروس + شهادات PDF، وفتح شراء الدورات لكل الأدوار).

**المتبقي الوحيد:** مفتاح UPayments Sandbox الحقيقي (بانتظار رد الدعم على +965 1809888) — بعده ننتقل من Mock Mode للدفع الفعلي.

صاحب المشروع مبتدئ بالبرمجة — اشرح كل خطوة بشكل مبسط قبل تنفيذها، واسأله قبل أي تغيير كبير أو حذف ملفات.

## عن المشروع
منصة تدريبية إلكترونية لمنظمة "إيليت" الكويتية (تدريب في علم النفس والتربية والتطوير الذاتي). الموقع الحالي ثابت (HTML/CSS/JS) ومستضاف على GitHub Pages، والهدف تحويله لمنصة كاملة فيها تسجيل دخول، دورات، دفع إلكتروني.

## اللغة والاتجاه
- المحتوى بالكامل عربي، اتجاه الصفحة RTL
- حافظ على نفس أقسام الموقع الأصلي: الرئيسية، عن إيليت، الأعضاء، الدورات، الاعتماد المهني، الفعاليات، بودكاست (استبصار)، المدونة، تواصل معنا
- لا تغيّر معلومات التواصل الموجودة (واتساب، إيميل، حسابات التواصل) إلا بطلب صريح

## البنية التقنية
- Next.js للواجهة + API Routes
- Supabase لقاعدة البيانات والمصادقة (Auth)
- Vercel للاستضافة
- UPayments API للدفع — **يُستدعى من الخادم فقط، أبدًا من كود المتصفح**
- Bunny Stream لاستضافة وتشغيل فيديوهات دروس الدورات

## الأدوار (Roles)
1. **student**: يشوف دوراته، شهاداته، يكتب تعليقات
2. **trainer**: داشبورد خاص (عدد المشتركين، الدخل لكل دورة، رسائل DM من الطلاب)
3. **admin**: مسار مخفي غير معلن، يدير الدورات/الأسعار/الخصومات، يشوف كل الإحصائيات
4. **staff** (دور جديد): أعضاء إدارة/موظفين — دور منفصل تمامًا عن trainer، له قسم خاص فيه:
   - مساحة ملفات خاصة بكل عضو (يرفع ويسترجع ملفاته الشخصية بأي وقت)
   - نوت/محرر نصوص شخصي محفوظ دائمًا **بتنسيق غني شبيه بـ Word فعليًا** (Bold، حجم الخط، نوع الخط، وأدوات تنسيق أساسية ثانية) يكتب فيه أي شي ويرجع له لاحقًا، مع إمكانية تحميله كملف (بصيغة يختارها العضو — txt حاليًا، PDF قيد الإضافة) محافظًا على نفس التنسيق، وحفظه اختياريًا بملفاته الشخصية
   - **أقسام/إدارات (departments):** الأدمن يعيّن كل عضو staff لقسم/إدارة معينة. فيه "ملفات القسم" (منفصلة عن الملفات الشخصية) ما يشوفها إلا أعضاء نفس القسم اللي عيّنهم الأدمن فيه (+ الأدمن نفسه)
   - تحميل النوت متوفر بصيغتي txt وPDF (محافظ على التنسيق، يدعم العربي RTL بمحوّل حقيقي بالخادم)
   - **مهام القسم (department tasks):** لكل قسم قائمة مهام فيها (عنوان، وصف اختياري، تاريخ استحقاق/deadline، checkbox إنجاز). الأدمن + الشخص المخوّل من الأدمن لإدارة ذاك القسم يقدرون يضيفون مهام جديدة. كل مهمة تُعيّن لعضو معيّن، وتظهر فقط لذاك الشخص (+ الأدمن)، مو لكل القسم. فقط الشخص المعيّنة له المهمة (أو الأدمن) يقدر يأشرها كمنجزة. مسؤول القسم يشوف فقط المهام اللي هو نفسه أنشأها (مو كل مهام القسم حتى لو أنشأها الأدمن)
   - **تقييم شهري (staff evaluations) — مكتمل:** الأدمن فقط يقدر يضيف/يعدّل/يحذف تقييم شهري لأي عضو staff، يتكوّن من: رقم أساسي من (1-12) + حقل منفصل "نقاط إضافية/بونس" من (0-2) + ملاحظة/تعليق نصي اختياري — الرقمين يظهرون بخط كبير وغامق بالواجهة. تقييم واحد بس لكل عضو بكل شهر (قيد unique، الأدمن يعدّل بدل ما يكرر). العضو نفسه يشوف تقييماته بنفسه من `/dashboard/staff` بس ما يقدر يعدّلها أو يحذفها إطلاقًا (ممنوع حتى من قاعدة البيانات). واجهة الأدمن بـ `/control-panel-2026/evaluations`. التنفيذ بملف `supabase/staff_evaluations.sql` (+ `staff_evaluations_fix_scale.sql` للتصحيح من مقياس 1-5 القديم لمقياس 1-12 + بونس)

## شراء الدورات مفتوح لكل الأدوار (مهم)
أي حساب (student, trainer, staff, admin) يقدر يشتري ويشترك بأي دورة، مو الطلاب بس. الدورات المشتراة تظهر لكل دور **داخل داشبورده الخاص هو** (قسم "دوراتي" مضاف لداشبورد المدرب/staff/الأدمن كل واحد بمكانه)، مو بإعادة توجيههم لصفحة داشبورد الطالب. منطق enrollments والتشغيل والشهادات يُعاد استخدامه كقطعة مشتركة (component) بين كل الداشبوردات.

## دروس الدورات — فيديو عبر Bunny Stream (ميزة جديدة، قيد التنفيذ)
كل دورة تقدر تحتوي درس واحد أو أكثر (فيديو). **الأدمن فقط** (مو المدرّب صاحب الدورة) يضيف/يعدّل/يحذف الدروس، من `/control-panel-2026/courses/[courseId]/lessons`. أي مشترك فعليًا بالدورة (بغض النظر عن دوره — راجع "شراء الدورات مفتوح لكل الأدوار" أعلاه) يشوف/يشغّل الفيديو، بنفس منطق جدول `enrollments`.

**كيف تشتغل الحماية:** رابط الفيديو المباشر ما يُخزّن ولا يُعرض أبدًا بالواجهة. عند الضغط على "تشغيل"، الواجهة تطلب من `/api/lessons/[lessonId]/playback` (خادم فقط) رابط تشغيل موقّع (SHA256 token + وقت انتهاء) صالح لساعتين بس — حتى لو انتسخ الرابط ما يشتغل بعد ما ينتهي. التحقق من الاشتراك يصير تلقائيًا عبر RLS على جدول `lessons` نفسه (نفس صف الدرس ما يرجع أصلاً لغير المشترك/المدرّب/الأدمن)، بدون فحص enrollments مكرر بالكود.

✅ **حالة الحماية:** تم تفعيل "Enable direct play" (معطّل) و"Token Authentication" (Embed view + CDN) بإعدادات مكتبة Bunny Stream، وتأكد بالاختبار الفعلي إن أي رابط بدون توكن صحيح يرجع خطأ 403 — الحماية فعّالة 100%.

التنفيذ بملف `supabase/lessons.sql`. يحتاج متغيرات بيئة `BUNNY_STREAM_LIBRARY_ID` و `BUNNY_STREAM_TOKEN_AUTH_KEY` بـ `.env.local`/Vercel. رفع الفيديو حاليًا يدوي: الأدمن يرفعه من لوحة Bunny.net نفسها وبعدين يلصق الـ Video ID بنموذج الدرس عندنا (بدل رفع مباشر من لوحتنا — قرار مقصود للبساطة بالمرحلة الحالية).

## إكمال الدروس + شهادات إتمام تلقائية (ميزة جديدة — الكود جاهز، بانتظار تشغيل SQL)
تحت كل درس فيديو checkbox "أنهيت مشاهدة هذا الدرس"، معطّل حتى ينتهي الفيديو فعليًا. بعد إكمال آخر درس بالدورة، يصدر النظام تلقائيًا شهادة PDF (قالب ذكر/أنثى حسب `users.gender`) معبّاة تلقائيًا، وتظهر بقسم "شهاداتي" بلوحة الطالب.

- **كشف انتهاء الفيديو:** المشغّل iframe خارجي (Bunny)، مو `<video>` عادي — نستخدم مكتبة Bunny الرسمية **Player.js** (`playerjs-latest.min.js` + `player.on("ended", ...)`) بدل `onEnded`/`onSeeked` اللي ما تشتغل مع iframe من مصدر خارجي.
- **التحقق من الإكمال:** يصير بالكامل بالسيرفر (`/api/lessons/[lessonId]/complete`) — نفس مبدأ حماية الدروس، الطلب يعتمد على RLS مو على الواجهة.
- **جدول `lesson_progress`:** سجل واحد لكل (مستخدم، درس)؛ سياسة INSERT تتحقق إن الدرس فعلاً متاح لهذا المستخدم (نفس شرط RLS بجدول `lessons`) — دفاع إضافي حتى لو حاول أحد يزوّر إكمال درس ما شافه.
- **توليد PDF الشهادة:** نفس أسلوب Puppeteer + خط Cairo المستخدم أصلاً بتصدير نوت staff (`src/lib/pdf.js` + دالة جديدة `renderFixedSizeHtmlToPdf` بدل مكتبات pdf-lib/arabic-reshaper خارجية) — القالب صورة خلفية + نصوص موضوعة فوقها بإحداثيات (`src/lib/certificate.js`). **الإحداثيات معايرة فعليًا على شهادة حقيقية مولّدة بنفس الخط (Cairo) ومقروءة بوضوح، مو تخمين بصري فقط** — ممكن تحتاج تحسين طفيف إضافي بعد ما تشوفها بسياق حقيقي.
- **الاسم بالشهادة (`certificate_name`):** يُسأل بكل عملية شراء دورة (نموذج `BuyCourseButton`)، يُخزّن على `payments` أولاً (يعيش خلال التحويل لـ UPayments والرجوع) وينتقل لـ `enrollments` بعد تأكيد الدفع.
- **الجنس (`users.gender`):** يُسأل مرة وحدة، **ببوابة قبل الشراء مباشرة** (مو بنموذج التسجيل) — يغطي الحسابات القديمة تلقائيًا بدون ما نحتاج نعدّل مسارين مختلفين للتسجيل (إيميل + Google).
- **توقيع المدرّب:** يرفعه الأدمن من `/control-panel-2026/courses` (بجانب السعر/النشر)، يُخزّن بـ bucket عام `trainer-signatures`.
- **الملفات:** `supabase/course_completion.sql` (جدول + أعمدة + bucket التوقيعات + bucket الشهادات — **لسا ما اتشغّل**)، `src/lib/certificate.js`، `src/app/api/lessons/[lessonId]/complete/route.js`.

## قسم "مواضيع الأعضاء" (ميزة جديدة)
قسم عام بالموقع، **يشوفه أي زائر حتى لو مو مسجل دخول** (مو خاص بالمستخدمين المسجلين فقط). الأدمن، وأي حساب يمنحه الأدمن الصلاحية، يقدر يرفق فيه ملفات.

## ترقية الحساب من student إلى trainer (مهم)
أي حساب student يقدر يطلب يصير trainer في أي وقت لاحقًا (مو بس وقت التسجيل)، لكن الترقية **لازم توافق عليها جهة مخوّلة** قبل ما تُفعّل. الموافقة مو حكرًا على الأدمن نفسه فقط:
- الأدمن دايمًا يقدر يوافق
- الأدمن يقدر يمنح صلاحية "الموافقة على طلبات المدربين" لحسابات ثانية (مثل مشرفين) بدون ما يعطيهم كامل صلاحيات الأدمن
- يعني نحتاج جدول لطلبات الترقية (trainer request) بحالة pending/approved/rejected، وعمود أو جدول صلاحيات منفصل يحدد مين عنده حق الموافقة غير الأدمن

### وضع مؤقت أثناء التطوير (قبل الإطلاق)
حاليًا ما فيه حساب أدمن فعلي بعد، فنحتاج طريقة سهلة نغيّر فيها دور أي حساب يدويًا (مباشرة من SQL Editor أو أداة داخلية بسيطة) بدون المرور بنظام الطلب/الموافقة، فقط لأغراض الاختبار أثناء البناء. **قبل نشر الموقع فعليًا للجمهور، لازم نتأكد إن هذا المسار المباشر يُلغى أو يُقيّد بالكامل**، وتصير الترقية فقط عبر نظام الطلب والموافقة الرسمي (أدمن أو حساب مخوّل من الأدمن).

## قواعد أمان صارمة
- مفتاح UPayments API يُخزّن فقط في متغيرات البيئة (`.env.local` محليًا، Environment Variables على Vercel) — **ممنوع يُكتب مباشرة بأي ملف كود أو يُرفع على GitHub**
- تأكد إن `.env.local` مذكور في `.gitignore`
- مسار الأدمن يجب يتحقق من الدور (`role === 'admin'`) في كل طلب، مو بس بالواجهة
- أي بيانات دفع أو مفاتيح API لا تظهر أبدًا في أدوات المطور (DevTools) بالمتصفح

## حالة المشروع الحالية
- [x] المرحلة 1: نظام تسجيل الدخول (إيميل/Google + الأدوار)
- [x] المرحلة 2: داشبورد المستخدم والمدرب
- [x] المرحلة 3: لوحة تحكم الأدمن
- [x] المرحلة 4: تكامل الدفع UPayments (بوضع Mock — بانتظار مفتاح Sandbox حقيقي من UPayments)
- [x] المرحلة 5: ربط الدومين eliteco.com.kw (شغّال فعليًا)
- [x] نظام staff بالكامل (الأقسام، الملفات، النوت الغني، PDF، مهام القسم، التقييم الشهري)
- [x] قسم "مواضيع الأعضاء" العام
- [x] دروس الدورات عبر Bunny Stream (الحماية فعّالة ومختبرة - Token Authentication مفعّل)
- [x] فتح شراء الدورات لكل الأدوار (مطبّق ومختبر)
- [x] إكمال الدروس + شهادات إتمام تلقائية (مكتمل — SQL مشغّل، جاهز للاختبار الحي)
- [x] مواضيع الأعضاء بالصيغة الجديدة (قسم #topics بالصفحة الرئيسية + إدارة أدمن + PDF viewer)

راجع ملف `PROJECT_PLAN.md` للتفاصيل الكاملة لكل مرحلة قبل البدء بأي جزء.

## ملاحظة للمطوّر (مبتدئ)
صاحب المشروع مبتدئ بالبرمجة — اشرح كل خطوة تسويها بشكل مبسّط، واسأل قبل تنفيذ أي تغيير كبير أو حذف بيانات.
